TubeJoint for Autodesk Inventor 2027
Iteration: 11
Package: iteration11-inventor-style-ui-v3
Template file: StandardSideTenonSlot_v6.ipt (preserved in place)
Template schema: 10, migrated by adding missing TJ_* parameters only
Ribbon tab: Design (id_TabDesign)
Window: floating Inventor DockableWindow, 620 x 790; no forced docking
Sizing workflow: Standard / Compact / Reinforced / Custom presets
Auto sizing: retained but disabled by default
Slot thickness: measured automatically from the male tube; no redundant UI row
Profile mode: solved tenon/slot sketches copied 1:1; no external X/Y/root scaling
Tenon contract: TJ_TenonLength / TJ_TenonAcrossWidth / TJ_TenonThickness
Reliefs: driven only by TJ_TenonRelief/TJ_SlotRelief inside the template
Preview: temporarily applies parameters, resamples all three solved IPT sketches, then restores the template
Curve support: typed GetPointAtParam sampling for Control Vertex Spline, ellipses and polylines
Final curves: the complete mixed contour is copied natively through Inventor and transformed as one temporary sketch block
Spline topology: CV and interpolation splines retain their native type and shared connections to lines/arcs
Native dependencies: CopyEntitiesTo control/fit entities remain grouped with their owning splines during placement
Male result silhouette: the profile bounding window is cleared first, then the exact TJ_MALE_PROFILE is joined back
Root attachment: a fixed 0.2 mm negative-X band is retained so the replacement profile intersects the original tube
Slot profiles: A/B use separate sketches and Cut features inside the same assembly transaction
Closed-chain fallback: a single connected closed path is accepted when AddForSolid rejects the same native contour
Topology repair: geometrically coincident neighboring spline/line/arc endpoints are merged as shared SketchPoints
Profile diagnostics: curve counts/types, failed chain seeds, broken joint index and geometric gap are reported
Creation diagnostics: named stages for template parameters, each tenon, slot/vent profile, Cut and document updates
Tenon extrusion: zero anchor-to-slot vector skips normalization and safely tries both Join directions
Mixed contours: lines/arcs use shared SketchPoints; oriented spline segments use exact fixed NURBS endpoints
Profile fallback: connected sketch curves are used when Inventor rejects source Profiles.AddForSolid
Spline endpoints: native contour copy preserves Inventor topology; service SketchPoint entities are ignored
Paired placement: A/B anchors solve wall plane, female plane and shared longitudinal station simultaneously
Vent creation: profile prepared before slot Cut; separate TJ_VENT_CUT uses one-sided female-wall depth
Paired roots: tenon A/B roots share one tube-axis station; slot intersections remain on the female plane
Primary wall selection: orientation by area, then outermost plane; never pair an inner wall with an outer wall
Vent size: explicit uniform 10-500 percent control; persisted in repository schema 12
Template guide: PARAMETRIC_TENON_SLOT_GUIDE_RU.md
Joint placement: native one-axis triad starts by default; no separate enable button
Hole: planar triad starts at the shifted joint and returns to the main triad after drag
Wall selection model: real opposite A/B and C/D discovery with 90-degree switching
Placement model: common joint and separate hole X/Y offsets persisted for native triads
Paired placement: opposite roots use the analytical tube-axis/female-plane station
Paired root bridge: opposite sides use the same maximum measured Trim/Notch gap and overlap
Angled joints: selected female-face-normal insertion direction retained
Repository schema: 12
UI: Icon.png with five inline editors; no spinner rectangles; compact hole controls
Tube recognition: straight rectangular and round hollow profiles from analytical B-Rep geometry
Round/RHS classification: round profiles require full-section diameter and coaxial inner/outer cylinders
Assembly batch: all unique recognized tube IPTs are normalized in one global transaction
Assembly preservation: every occurrence gets T_old * inverse(normalization); range boxes are verified to 0.05 mm
Tube file naming: optional SIZE x WALL_OLD-NAME_QUANTITY шт.ipt
Nominal formatting: section and wall rounded to 0.1 mm; trailing decimal zero omitted
Rename safety: IAM references switch through SaveAs; original IPT files remain as backups
Tube properties: Description/Stock Number plus stable TubeJoint.* custom iProperties
Tube normalization: OBB centre to origin; length/width/height aligned to +Z/+X/+Y by one Move feature
Recognition safety: exactly one writable solid body, hollow-profile validation and pre-apply review
Tube preparation UI: per-file review table with selection, exact/nominal dimensions and typed failure status
Rename UI: separate opt-in checkbox, unchecked by default; unsaved selected IPTs cannot be renamed
Inventor-style UI: 330 px property pane, compact vertical rows, current Inventor theme palette
Docking: left/right docking remains available; remembered user pane state is preserved
High-DPI sizing: child control no longer doubles the DockableWindow minimum width
Segment controls: owner-drawn selected state retains the Inventor blue accent
Pane migration: V7 resets the oversized V6 geometry once, then preserves new user sizing
Preset control: focused closed value keeps the native dark editor background
Cancel: DockableWindow X stops triads/timer, deletes stale preview graphics and resets the command
Undo: failed global transactions are always aborted; template working values are restored without save
Auto slot thickness: measured male-wall value bypasses manual standard-step validation
Partial preview: one invalid template sketch no longer hides every valid preview body
